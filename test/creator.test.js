import * as actions from '../../actions/userActions'

describe('actions', () => {
	it('should create and action to create a todo', () => {
		const expectedAction = {
			type: ,
			payload: null
		}
		expect(actions.loginUser()).toEqual(expectedAction)
	})
})